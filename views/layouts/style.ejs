<!-- Favicon -->
<link rel="shortcut icon" type="image/x-icon" href="assets/img/favicon.png" />

<!-- Owl Carousel CSS -->

<% if(active_path === routes.file_manager) { %>
<link rel="stylesheet" href="assets/css/plyr.css">
<% } %>

<% if(active_path === routes.class_time_table || active_path === routes.file_manager || active_path === routes.notes || active_path === routes.parent_dashboard || active_path === routes.student_dashboard || active_path === routes.student_fees || active_path === routes.student_library || active_path === routes.student_result || active_path === routes.student_time_table || active_path === routes.teacher_dashboard || active_path === routes.todo ) { %>
<link rel="stylesheet" href="assets/css/owl.carousel.min.css">
<% } %>

<% if(active_path === routes.activities || active_path === routes.application_call || active_path === routes.admin_dashboard || active_path === routes.layout_box || active_path === routes.layout_dark || active_path === routes.layout_default || active_path === routes.layout_mini || active_path === routes.layout_rtl || active_path === routes.video_call  ) { %>
<link rel="stylesheet" href="assets/plugins/owlcarousel/owl.carousel.min.css" />
<link rel="stylesheet" href="assets/plugins/owlcarousel/owl.theme.default.min.css"/>
<% } %>


<!-- Tabler Icon CSS -->
<link rel="stylesheet" href="assets/plugins/tabler-icons/tabler-icons.css">   

<% if(active_path === routes.ui_clipboard || active_path === routes.ui_drag_drop) { %>
<!-- Dragula CSS -->
<link rel="stylesheet" href="assets/plugins/dragula/css/dragula.min.css">
<% } %>

<!-- Bootstrap CSS -->
<link rel="stylesheet" href="assets/css/bootstrap.min.css" />

<% if(active_path === routes.form_wizard) { %>
<!-- Wizard CSS -->
<link rel="stylesheet" href="assets/plugins/twitter-bootstrap-wizard/form-wizard.css">
<% } %>

<!-- Select2 CSS -->
<link rel="stylesheet" href="assets/plugins/select2/css/select2.min.css" />

<!-- Animation CSS -->
<link rel="stylesheet" href="assets/css/animate.css" />

<% if(active_path === routes.application_chat) { %>
<!-- Fancybox -->
<link rel="stylesheet" href="assets/plugins/fancybox/jquery.fancybox.min.css">
<% } %>

<!-- Datatable CSS -->
<link rel="stylesheet" href="assets/css/dataTables.bootstrap5.min.css" />

<!-- Fontawesome CSS -->
<link rel="stylesheet" href="assets/plugins/fontawesome/css/fontawesome.min.css"/>
<link rel="stylesheet" href="assets/plugins/fontawesome/css/all.min.css" />

<% if(active_path === routes.ui_rangeslider || active_path === routes.ui_rating ) { %>
<!-- Rangeslider CSS -->
<link rel="stylesheet" href="assets/plugins/ion-rangeslider/css/ion.rangeSlider.min.css"/>
<% } %>

<% if(active_path === routes.application_chat) { %>
<!-- Mobile CSS-->
<link rel="stylesheet" href="assets/plugins/intltelinput/css/intlTelInput.css">
<link rel="stylesheet" href="assets/plugins/intltelinput/css/demo.css">
<% } %>

<% if(active_path === routes.add_invoice || active_path === routes.add_student || active_path === routes.add_staff || active_path === routes.add_teacher || active_path === routes.custom_fields || active_path === routes.edit_invoice || active_path === routes.edit_staff || active_path === routes.edit_student || active_path === routes.edit_teacher || active_path === routes.invoice || active_path === routes.localization) { %>
<!-- Bootstrap Tagsinput CSS -->
<link rel="stylesheet" href="assets/plugins/bootstrap-tagsinput/bootstrap-tagsinput.css">
<% } %>


<!-- Daterangepikcer CSS -->
<link rel="stylesheet" href="assets/plugins/daterangepicker/daterangepicker.css">

<% if(active_path === routes.ban_ip_address || active_path === routes.application_chat || active_path === routes.email_templates || active_path === routes.file_manager || active_path === routes.gdpr_cookies || active_path === routes.notes || active_path === routes.religion || active_path === routes.school_settings || active_path === routes.sms_settings || active_path === routes.storage || active_path === routes.todo || active_path === routes.ui_text_editor ) { %>
<!-- Summernote CSS -->
<link rel="stylesheet" href="assets/plugins/summernote/summernote-bs4.min.css"/>
<% } %>

<% if(active_path === routes.ui_lightbox) { %>
<!-- animation CSS -->
<link rel="stylesheet" href="assets/plugins/lightbox/glightbox.min.css">
<% } %>

<% if(active_path === routes.add_invoice || active_path === routes.blog_categories || active_path === routes.blog_comments || active_path === routes.blog_tags || active_path === routes.blog || active_path === routes.edit_invoice || active_path === routes.invoice || active_path === routes.ticket_grid ) { %>
<!-- Summernote CSS -->
<link rel="stylesheet" href="assets/plugins/summernote/summernote-lite.min.css">
<% } %>

<% if(active_path === routes.ui_scrollbar || active_path === routes.ui_spinner ) { %>
<!-- Main CSS -->
<link rel="stylesheet" href="assets/plugins/scrollbar/scroll.min.css" />
<% } %>

<% if(active_path === routes.ui_stickynote || active_path === routes.ui_timeline ) { %>
<!-- Sticky CSS -->
<link rel="stylesheet" href="assets/plugins/stickynote/sticky.css" />
<% } %>

<!-- Feather CSS -->
<link rel="stylesheet" href="assets/plugins/icons/feather/feather.css" />

<% if(active_path === routes.application_call || active_path === routes.form_vertical || active_path === routes.tables_basic || active_path === routes.ui_alerts || active_path === routes.ui_nav_tabs || active_path === routes.video_call  ) { %>
<!-- Feathericon CSS -->
<link rel="stylesheet" href="assets/css/feather.css">
<% } %>

<% if(active_path === routes.icon_ionic) { %>
<!-- Ionic CSS -->
<link rel="stylesheet" href="assets/plugins/icons/ionic/ionicons.css" />
<% } %>

<% if(active_path === routes.icon_material) { %>
<!-- Material CSS -->
<link rel="stylesheet" href="assets/plugins/material/materialdesignicons.css" />
<% } %>

<% if(active_path === routes.icon_pe7) { %>
<!-- Pe7 CSS -->
<link rel="stylesheet" href="assets/plugins/icons/pe7/pe-icon-7.css" />
<% } %>

<% if(active_path === routes.icon_simpleline) { %>
<!-- Material CSS -->
<link rel="stylesheet" href="assets/plugins/simpleline/simple-line-icons.css" />
<% } %>

<% if(active_path === routes.icon_themify) { %>
<!-- themify CSS -->
<link rel="stylesheet" href="assets/plugins/icons/themify/themify.css" />
<% } %>

<% if(active_path === routes.icon_weather) { %>
<!-- weather CSS -->
<link rel="stylesheet" href="assets/plugins/icons/weather/weathericons.css" />
<% } %>

<% if(active_path === routes.icon_typicon) { %>
<!-- Typicons CSS -->
<link rel="stylesheet" href="assets/plugins/icons/typicons/typicons.css" />
<% } %>

<% if(active_path === routes.icon_flag) { %>
<!-- Flag CSS -->
<link rel="stylesheet" href="assets/plugins/icons/flags/flags.css" />
<% } %>

<% if(active_path === routes.chart_morris) { %>
<!-- Morris CSS -->
<link rel="stylesheet" href="assets/plugins/morris/morris.css">
<% } %>

<!-- Datetimepicker CSS -->
<link rel="stylesheet" href="assets/css/bootstrap-datetimepicker.min.css" />

<% if(active_path === routes.call_history || active_path === routes.application_call || active_path === routes.application_chat || active_path === routes.video_call ) { %>
<!-- Boxicons CSS -->
<link rel="stylesheet" href="assets/plugins/boxicons/css/boxicons.min.css" />
<% } %>

<% if(active_path === routes.application_chat || active_path === routes.video_call ) { %>
<!-- Swiper CSS -->
<link rel="stylesheet" href="assets/plugins/swiper/swiper.min.css" />
<% } %>

<% if(active_path === routes.layout_rtl) { %>
<!-- Bootstrap CSS -->
<link rel="stylesheet" href="assets/css/bootstrap.rtl.min.css">
<% } %>

<!-- Main CSS -->
<link rel="stylesheet" href="assets/css/style.css" />

